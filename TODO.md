# TODO

- [x] Remplacer le contenu de `src/main/resources/templates/cheptel/dashboard.html` par le design inspiré de `CHEPTELle.html`.
- [x] Intégrer les liens CRUD (œil/modifier/supprimer) pour que les boutons marchent.
- [ ] Implémenter les endpoints manquants côté pages dans `CheptelPageController` (/edit, /delete, POST nouveau, etc.).
- [ ] Raccorder le formulaire “Ajouter une vache” à la route web correcte.

- [ ] Ajouter les `INSERT INTO` dans `base.sql` pour seed `ref_race` et `ref_statut_vache`.

- [ ] Lancer l’app et vérifier le tout (navigation + CRUD + seed en base).

